import { StyledComponent } from "./styles";

type Props = object;

function PaymentMethods(props: Props) {
  return <StyledComponent>PaymentMethods</StyledComponent>;
}

export default PaymentMethods;
