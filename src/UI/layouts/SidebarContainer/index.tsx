import { StyledComponent } from "./styles";

type Props = object;

function SidebarContainer(props: Props) {
  return <StyledComponent>SidebarContainer</StyledComponent>;
}

export default SidebarContainer;
